# Лабораторная работа 2

## «Нейросетевой перевод на базе Hugging Face Transformers (MarianMT, NLLB)»

**Курс:** «Основы машинного перевода» · **Разделы:** 2 и 4
**Максимум: 20 баллов** · **Срок сдачи:** 01.11.2026
**Среда выполнения:** Google Colab (Python 3.10+, GPU T4)
**Сквозной кейс:** та же предметная область и тот же вариант, что в ЛР № 1. Входные данные —
артефакты ЛР № 1: терминологическая база `variant_<N>.tbx` и память переводов
`variant_<N>.tmx`.

---

## 1. Цель и формируемые компетенции

**Цель** — развернуть NMT-пайплайн в Colab, исследовать субсловную токенизацию, батчинг,
условную кросс-энтропию и механизм внимания, а также сравнить специализированную EN→RU модель
**MarianMT** с многоязычной **NLLB** на материале собственного варианта.

| Компетенция | Что проверяется |
|---|---|
| **ОПК-2** | применение NMT при создании локализованных образовательных материалов |
| **ОПК-9** | программное использование NLP-моделей и библиотек |
| **УК-1** | анализ поведения двух моделей и причин переводческих ошибок |
| **УК-6** | организация воспроизводимого эксперимента с фиксированными данными и версиями |

### Место работы в курсе

```
ЛР № 1  Память переводов и CAT         ЛР № 2  Нейросетевой перевод        далее по курсу
──────────────────────────────         ────────────────────────────        ──────────────────────
TBX: терминология              ───►    токенизация терминов, QA      ───►  метрики качества
TMX: параллельные сегменты     ───►    вход моделей + эталон               BLEU, chrF, COMET
Fuzzy Match / ICE: повторное           кросс-энтропия, внимание            постредактирование
использование перевода                 сравнение Marian и NLLB
```

ЛР № 1 отвечала на вопрос «как **повторно использовать** уже выполненный перевод», ЛР № 2 —
«что будет, если перевод **породит модель**», следующая работа курса — «**насколько хорошо**
это получилось». Поэтому в ЛР № 2 сознательно нет метрик качества: кросс-энтропия их не
заменяет.

---

## 2. Входные данные: артефакты ЛР № 1

Работа продолжает **тот же индивидуальный вариант**, что и ЛР № 1, и использует файлы, которые
были сданы по её итогам. Ничего нового собирать не нужно.

| Файл ЛР № 1 | Что внутри | Во что превращается в ЛР № 2 |
|---|---|---|
| `variant_<N>.tmx` | единицы перевода `<tu tuid>`: `<seg>` на en и ru, `prop[@type="domain"]`, порядок сегментов | `segments_en.csv` (`segment_id`, `source`) и `aligned_reference.csv` (+ `reference`) |
| `variant_<N>.tbx` | `termEntry` → `langSet` en/ru → `<term>` (варианты `<tig>` и `<ntig><termGrp>`) | `glossary.csv` (`en`, `ru`) |
| `tm_segments_v<N>.csv` | `tuid`, `source_segment`, `target_segment`, `domain` | резервный путь вместо TMX |
| `glossary_smartcat_v<N>.csv` | `Source (en)`, `Target (ru)`, `Comment` | резервный путь вместо TBX |
| `fuzzy_report_v<N>.csv` | отчёт Fuzzy Match и ICE | в расчётах не участвует; пригодится в выводах |

Соответствие полей буквальное: `tuid` → `segment_id`, английский `<seg>` → `source`, русский
`<seg>` → `reference`, английский `<term>` → `en`, русский `<term>` → `ru`. Преобразование
выполняет **блок 2.0 ноутбука** (функции `tmx_to_pairs()`, `tbx_to_glossary()`,
`build_lab02_inputs()`) — писать его не нужно.

> **XML предпочтительнее CSV.** В `.tmx` и `.tbx` сохранены коды языков, порядок сегментов и
> предметная область; CSV-выгрузка часть этого теряет. Кладите в каталог данных именно
> `variant_<N>.tmx` и `variant_<N>.tbx`.

### Требуемый объём и как его добрать

| Ресурс | Минимум для сдачи |
|---|---|
| Сегментов в памяти переводов (`<tu>`) | **120** |
| Терминов в терминологической базе (`termEntry`) | **100** |

В ЛР № 1 корпус мог быть меньше (в эталоне варианта 25 — 14 сегментов и 18 терминов). Тогда
откройте **свой** ноутбук ЛР № 1, дополните списки `PARALLEL_CORPUS` и `TERMS` материалами той
же предметной области, перегенерируйте `variant_<N>.tmx` и `variant_<N>.tbx` и перенесите их в
ЛР № 2. Код ЛР № 1 при этом не меняется — меняются только данные. Русские соответствия в
добавленной части проверяются вручную: `reference` должен остаться переводом, подтверждённым
человеком, иначе расчёт кросс-энтропии теряет смысл.

### Как подключить файлы в Colab

**Способ 1 — загрузка с компьютера:**

```python
from google.colab import files
for name, content in files.upload().items():   # выбрать variant_<N>.tmx и variant_<N>.tbx
    (DATA / name).write_bytes(content)
```

**Способ 2 — Google Drive** (удобнее, если работа идёт в несколько сессий):

```python
from google.colab import drive
drive.mount("/content/drive")
import shutil, glob
for path in glob.glob("/content/drive/MyDrive/lab01/variant_*.t*x"):
    shutil.copy(path, DATA)
```

> Файлы Colab **не сохраняются** между сессиями: после отключения среды каталог `/content`
> очищается. Скачивайте архив с результатами до закрытия вкладки (последний блок) или храните
> рабочие файлы на Google Drive.

**Если артефакты ЛР № 1 не найдены,** студенческий ноутбук создаёт мини-набор из четырёх пар и
четырёх терминов — он годится только для проверки автотестов, сдавать работу на нём нельзя.
Преподавательский ноутбук в этом случае использует встроенный эталонный комплект варианта 25.

---

## 3. Требования к среде

* **Runtime → Change runtime type → T4 GPU.** На 120 сегментах и двух моделях работа без GPU
  занимает часы: NLLB-600M на CPU выдаёт примерно 3–6 с на сегмент.
* Первый запуск скачивает около **2.7 ГБ** весов (MarianMT ≈ 300 МБ, NLLB-600M ≈ 2.4 ГБ).
* Ориентировочное время на T4: перевод 120 сегментов двумя моделями с батчингом — 3–6 минут,
  весь ноутбук целиком — 15–25 минут вместе с загрузкой моделей.
* Установка (в Colab `torch` уже есть):

```python
!pip install -q "transformers>=4.40" sentencepiece sacremoses
```

`sentencepiece` требуется токенизатору NLLB, `sacremoses` — предобработке MarianMT. Разбор
TBX и TMX выполняется через `lxml` (в Colab предустановлен), при его отсутствии — через
`xml.etree.ElementTree`.

**Модели:**

* `Helsinki-NLP/opus-mt-en-ru` — специализированная EN→RU, ≈ 76 млн параметров,
  токенизатор `MarianTokenizer`, словарь 62 518 единиц;
* `facebook/nllb-200-distilled-600M` — многоязычная, 200 языков, ≈ 615 млн параметров,
  токенизатор `NllbTokenizer`, словарь 256 204 единицы, дистиллированная версия большой
  модели NLLB-200.

Документация NLLB: <https://huggingface.co/docs/transformers/model_doc/nllb>

**Языковые коды NLLB:** `src_lang="eng_Latn"`, целевой язык задаётся при генерации через
`forced_bos_token_id=tokenizer.convert_tokens_to_ids("rus_Cyrl")`. Технически это означает, что
декодер обязан начать вывод со специального токена целевого языка; без него язык результата не
определён — модель может выдать перевод на любой из двухсот языков.

### Две особенности актуальных версий библиотек

1. **`attn_implementation="eager"` обязателен для блока с вниманием.** Начиная с
   `transformers` 4.36 при PyTorch ≥ 2.1.1 по умолчанию подключается ускоренная реализация
   внимания **SDPA**, которая не обязана сохранять веса. В зависимости от версии библиотека
   либо сама откатывается на `eager` с предупреждением, либо возвращает пустой кортеж
   `outputs.cross_attentions`, и код падает с `IndexError: tuple index out of range`. Модель
   нужно загружать так:

```python
model = AutoModelForSeq2SeqLM.from_pretrained(name, attn_implementation="eager")
```

   Старые версии библиотеки такого аргумента не знают — оборачивайте вызов в
   `try / except (TypeError, ValueError)`.

2. **Предупреждение о `max_new_tokens` и `max_length`.** В `generation_config` моделей задан
   `max_length=512`, поэтому при каждом вызове `generate(max_new_tokens=...)` печатается
   предупреждение. Приоритет у `max_new_tokens`, результат корректен; чтобы не зашумлять
   вывод, в начале ноутбука вызывается `transformers.logging.set_verbosity_error()`.

---

## 4. Ход работы

| Шаг | Что делается | Блок студенческого ноутбука | Блок эталона |
|---:|---|---|---|
| 1 | Определение устройства, фиксация зерна, структура каталогов | 1 | 1 |
| 2 | Подключение артефактов ЛР № 1 и их разбор в таблицы | 2, 2.0 | 2, 2.0 |
| 3 | Загрузка MarianMT (`eval()`, `eager`) | 3 | 3 |
| 4 | Субсловная токенизация 100+ терминов глоссария | 5, 12 | 4, 10 |
| 5 | Перевод одного сегмента, параметры декодирования | 4 | 5 |
| 6 | Батчинг: последовательный и пакетный инференс, замер времени | 10 | 6 |
| 7 | Условная кросс-энтропия для 5+ пар `source` / `reference` | 6, 13 | 7 |
| 8 | Cross-attention для 2+ пар, форма тензора, усреднение голов | 7, 13 | 8 |
| 9 | Загрузка NLLB, языковые коды, контрольный запуск без `forced_bos_token_id` | 3, 4 | 9 |
| 10 | Сравнение токенизаторов Marian и NLLB | 12 | 10 |
| 11 | Полный перевод модуля обеими моделями в JSONL | 11 | 11 |
| 12 | Проверка терминологии по глоссарию ЛР № 1 (glossary QA) | 8, 14 | 12 |
| 13 | Сводная таблица `comparison.csv` и экспертный выбор | 14 | 13 |
| 14 | Анализ различий и ошибок, отчёт, выгрузка результатов | 15–17 | 14–15 |

Перед блоками 0–2 в обоих ноутбуках добавлены разделы «Суть работы за пять минут», «Связь с
ЛР № 1 и место работы в курсе» и «Теоретическое обоснование» — их следует прочитать до запуска
кода. В конце ноутбуков приведён список литературы для углублённого изучения.

### Ключевые формулы и понятия

Условная кросс-энтропия целевой строки при данном источнике:

```math
\mathcal{L} = -\frac{1}{T}\sum_{t=1}^{T}\log P\left(y_t \mid y_{1:t-1}, x\right)
```

Строка оценивается в режиме **teacher forcing**: модель получает готовый перевод и на каждом
шаге оценивает вероятность фактического следующего токена при правильном префиксе. Величина
нормирована на длину, паддинг исключён меткой `-100`; удобная эквивалентная форма — перплексия
`exp(L)`. Меньший loss означает, что строка более вероятна для данной модели. **Loss не
является метрикой качества перевода** и несопоставим между моделями с разными словарями.

Форма тензора cross-attention: `[batch, heads, target_len, source_len]`. Внимание —
**диагностический сигнал**, а не доказательство того, что конкретная голова «выучила правило
перевода».

О токенизации: не делайте механический вывод «Marian = BPE, NLLB = SentencePiece» — это
сравнение алгоритма с библиотекой-обёрткой, которая умеет и BPE, и Unigram. Проверяйте класс
токенизатора, model card и config; в актуальной документации Hugging Face токенизатор NLLB
описан как основанный на **Unigram**.

---

## 5. Обязательный минимум

1. перевести **120+ одинаковых сегментов** своей памяти переводов с помощью MarianMT;
2. перевести те же **120+ сегментов** с помощью NLLB;
3. исследовать токенизацию **100+ терминов** своей терминологической базы обоими токенизаторами;
4. вычислить cross-entropy минимум для **5 пар** `source` / `reference`;
5. исследовать cross-attention минимум для **2 пар**;
6. измерить последовательный и пакетный инференс (не менее двух размеров пачки);
7. выполнить glossary QA с ручным подтверждением нарушений;
8. выделить минимум **5 содержательных различий или ошибок** между моделями;
9. для каждого из пяти случаев аргументировать предпочтительный перевод;
10. построить не менее **4 графиков** и сохранить их в `figures/`;
11. сохранить результаты в воспроизводимом виде (`JSONL` / `CSV` + ноутбук + `requirements.txt`).

---

## 6. Варианты

**Отдельной таблицы вариантов у ЛР № 2 нет.** Номер варианта, предметная область и
терминология полностью наследуются из ЛР № 1: работа сквозная, и вход ЛР № 2 — это выход
ЛР № 1. Перечень тем смотрите в методических материалах ЛР № 1.

| Что задаёт вариант | Где это видно в ЛР № 2 |
|---|---|
| Предметная область и терминология | `variant_<N>.tbx` → анализ токенизации и glossary QA |
| Параллельный корпус и эталонный перевод | `variant_<N>.tmx` → вход моделей, кросс-энтропия, сравнение |
| Контекстно неоднозначные единицы домена | блок анализа различий: именно они чаще всего дают ошибки |

Предметная область считывается из самих файлов: в TMX это `prop[@type="domain"]`, в TBX —
`descrip[@type="subjectField"]`; блок 2.0 печатает её при загрузке. Если нумерация в других
материалах курса расходится, ориентируйтесь на предметную область своего комплекта TBX/TMX,
а не на номер.

**Эталонное решение преподавателя** (`lab_02_teacher.ipynb`) выполнено по **варианту 25 —
«Авиастроение и беспилотные авиационные системы: телеметрия, БАС и авионика»**, тому же, что в
эталоне ЛР № 1. Комплект `variant_25.tbx` и `variant_25.tmx` встроен в блок 2 ноутбука
дословно, поэтому эталон воспроизводится без внешних файлов. Его объём (14 сегментов,
18 терминов) намеренно меньше студенческого минимума: эталон демонстрирует методику, а не
объём.

---

## 7. Результаты работы

После выполнения ноутбука в Colab создаётся каталог `/content/lab02`:

```text
/content/lab02/
├── data/
│   ├── variant_<N>.tbx                ← артефакт ЛР № 1 (вход)
│   ├── variant_<N>.tmx                ← артефакт ЛР № 1 (вход)
│   ├── segments_en.csv                ← собрано блоком 2.0
│   ├── glossary.csv                   ← собрано блоком 2.0
│   ├── aligned_reference.csv          ← собрано блоком 2.0
│   ├── source_en.md
│   └── reference_ru.md
├── results/
│   ├── results_marian.jsonl
│   ├── results_nllb.jsonl
│   ├── comparison.csv
│   ├── tokenization_10_terms.csv      ← таблица по всем терминам глоссария
│   ├── loss_5_pairs.csv
│   ├── attention_marian_<segment_id>.csv
│   ├── attention_summary.csv
│   ├── timing.csv
│   └── glossary_qa.csv
├── figures/
│   ├── timing.png
│   ├── tokenization_comparison.png
│   ├── loss.png
│   ├── cross_attention.png
│   └── glossary_qa.png
├── report.md
├── requirements.txt
└── lab02_results.zip                  ← архив для скачивания
```

Поля `preferred` и `comment` в `comparison.csv`, а также `confirmed` и `comment` в
`glossary_qa.csv` заполняются **вручную** после экспертного анализа. Это принципиальная часть
работы, и автоматический выбор модели её не заменяет.

Для сдачи в GitHub-репозиторий содержимое архива раскладывается в каталог `lab02/`, туда же
добавляется сам ноутбук `LAB_02.ipynb`.

### Содержание `report.md`

1. среда и устройство (включая тип GPU в Colab и версии `torch` / `transformers`);
2. model id обеих моделей, классы токенизаторов, размеры словарей;
3. таблица токенизации и график сравнения дробления терминов;
4. 120+ пар переводов (файлы JSONL) и примеры расхождений;
5. loss для 5 примеров и график;
6. форма тензора и анализ attention с тепловой картой;
7. время инференса в двух режимах и график;
8. 5+ ошибок или содержательных различий с аргументацией;
9. glossary compliance с указанием подтверждённых нарушений и false positive;
10. вывод.

Заголовок отчёта, тема и раздел с различиями собираются автоматически из данных прогона:
номер варианта и предметная область подставляются из загруженных артефактов ЛР № 1, а
перечень различий — из заполненных вами колонок `preferred` и `comment`.

---

## 8. Критериальный лист (20 баллов)

| Критерий | Состав критерия | Баллы |
|---|---|---:|
| **Работоспособность пайплайна/кода** | MarianMT — 2 · NLLB — 2 · batching/loss/attention — 2 | **6** |
| **Корректность лингвистической обработки и валидации** | языковые коды и токенизация — 2 · объём корпуса (120+ сегментов, 100+ терминов) — 1 · glossary QA — 2 | **5** |
| **Анализ ошибок и аргументация решений** | 5+ различий моделей — 2 · корректная интерпретация loss/attention — 2 · вывод — 1 | **5** |
| **Оформление, воспроизводимость** | результаты и 4+ графика — 1 · README — 1 · зависимости — 1 · воспроизводимая структура — 1 | **4** |
| **Итого** |  | **20** |

**Снижения:**

* объём корпуса меньше 120 сегментов или 100 терминов — −2 балла;
* использованы демонстрационный мини-набор или встроенный эталонный комплект варианта 25
  вместо собственных артефактов ЛР № 1 — −3 балла;
* колонки `preferred` / `comment` не заполнены или заполнены автоматически — −2 балла;
* ноутбук не выполняется целиком (`Runtime → Run all`) — −3 балла;
* графики отсутствуют или их меньше четырёх — −1 балл;
* вывод ячеек не сохранён — −1 балл.

---

## 9. Типичные ошибки

| Ошибка | Следствие | Как избежать |
|---|---|---|
| Поиск в результатах ЛР № 1 файлов `segments_en.csv` или `glossary.csv` | их там нет: ЛР № 1 выдаёт TBX, TMX и выгрузки для Smartcat | класть в каталог данных `variant_<N>.tmx` и `variant_<N>.tbx`, остальное соберёт блок 2.0 |
| Работа продолжена на демонстрационном мини-наборе | выводы недостоверны, работа не принимается | дождаться в выводе блока 2.0 имён своих файлов и предметной области своего варианта |
| Вместо XML загружены только CSV-выгрузки | теряются порядок сегментов, коды языков и домен | использовать CSV только как резервный путь |
| Модель загружена без `attn_implementation="eager"` | `outputs.cross_attentions` пуст, `IndexError: tuple index out of range` | передавать аргумент при загрузке и проверять `cross_attentions` перед индексированием |
| Не задан `forced_bos_token_id` для NLLB | перевод не на тот язык | получить код через `convert_tokens_to_ids("rus_Cyrl")` |
| Модель не переведена в `eval()` | нестабильный результат между запусками | вызвать `model.eval()` сразу после загрузки |
| Замер времени без прогрева | завышенное время первого режима | сделать один холостой вызов до замеров |
| `add_special_tokens=True` при анализе токенизации | в разборе появляются `</s>` и код языка | явно передать `add_special_tokens=False` |
| Loss интерпретируется как качество | неверные выводы | loss — вероятность строки; качество измеряется в ЛР № 4 |
| Сравнение loss между Marian и NLLB | некорректный вывод | словари различаются вчетверо, значения несопоставимы |
| Знаменатель glossary QA — все пары «сегмент × термин» | завышенная доля соблюдения | считать только фактические вхождения терминов в исходный текст |
| Строковый glossary QA без ручной проверки | ложные срабатывания из-за русской морфологии | подтверждать каждое нарушение в колонке `confirmed` |
| Все 100+ терминов на одном графике | нечитаемая диаграмма | отбирать 20–30 показательных терминов |
| Работа закрыта до скачивания архива | результаты потеряны вместе со средой | скачать `lab02_results.zip` или хранить файлы на Drive |

---

## 10. Состав методических материалов

| Файл | Назначение |
|---|---|
| `readme_lab_02.md` | настоящее описание работы |
| `lab_02.ipynb` | студенческая заготовка для Colab: теоретические разделы, адаптер артефактов ЛР № 1, шаблоны функций, автотесты, блоки для самостоятельного выполнения и построения графиков, список литературы |
| `lab_02_teacher.ipynb` | эталонное решение варианта 25: встроенный комплект ЛР № 1, полный пайплайн, пояснения к каждому блоку, графики, генерация всех результирующих файлов, методические указания для преподавателя |

Оба ноутбука содержат одинаковые теоретические разделы («Суть работы за пять минут», «Связь с
ЛР № 1 и место работы в курсе», «Теоретическое обоснование») и общий список литературы: от
учебника Koehn по NMT и работ по субсловной токенизации (Sennrich, Kudo) до статей об
интерпретации внимания (Jain & Wallace, Wiegreffe & Pinter) и стандартов ISO 17100 и
ISO 18587 по переводческим услугам и постредактированию машинного перевода.
