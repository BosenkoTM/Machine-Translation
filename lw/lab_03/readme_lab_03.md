# Лабораторная работа 3. «Контекстный перевод и локализация с помощью LLM (GigaChat / Yandex AI Studio / локальная LLM)»

**Курс:** «Основы машинного перевода»

**Максимум: 20 баллов**

**Среда выполнения:** Google Colab (для локальной модели — GPU T4) или JupyterLab

**Сквозной кейс:** та же предметная область и тот же вариант, что в ЛР № 1 и ЛР № 2. Вход —
память переводов и терминологическая база из ЛР № 1, дополнительная точка сравнения —
переводы MarianMT и NLLB из ЛР № 2.

---

## 1. Цель и формируемые компетенции

**Цель** — построить управляемый LLM-пайплайн локализации, в котором перевод задаётся системной
инструкцией, терминологическими ограничениями сегмента, примерами few-shot и JSON-схемой, а
результат проверяется автоматически и точечно исправляется.

| Компетенция | Что проверяется |
|---|---|
| **ОПК-2** | применение генеративного ИИ для локализации образовательного контента |
| **ОПК-9** | использование API и локальных LLM в цифровом образовательном процессе |
| **УК-1** | критическая проверка контекстных, стилистических и терминологических решений модели |
| **УК-6** | документирование промптов, параметров, версий и повторяемости эксперимента |

### Место работы в курсе

```
ЛР № 1  TMX + TBX            ЛР № 2  NMT (Marian, NLLB)        ЛР № 3  LLM-пайплайн             ЛР № 4
повторное использование  ─►  перевод без управления      ─►  перевод с инструкцией,      ─►  метрики качества
перевода                     терминология «как выйдет»       терминами, форматом, repair      BLEU, chrF, COMET
```

Метрики качества (BLEU, chrF, COMET) в ЛР № 3 не считаются — это тема ЛР № 4. Качество здесь
оценивается по соблюдению глоссария, сохранению разметки и экспертному разбору.

---

## 2. Что изменено в постановке и почему

| Было | Стало | Причина |
|---|---|---|
| Таблица вариантов 1–10 по информатике и демонстрационный вариант 11 | вариант и предметная область наследуются из ЛР № 1; эталон — **вариант 25** | работа сквозная; эталоны ЛР № 1 и ЛР № 2 выполнены по варианту 25 (авиастроение и БАС), темы по информатике с ЛР № 1 не совпадали |
| Вход: `source_en.md`, `glossary.csv`, `memory.tmx`, `reference_ru.md` | вход: `variant_<N>.tmx`, `variant_<N>.tbx` из ЛР № 1 и `lab02_results.zip` из ЛР № 2 | файлов `memory.tmx` и `source_en.md` в результатах ЛР № 1 нет; разбор выполняет тот же адаптер, что в ЛР № 2 |
| В промпт передаётся весь глоссарий | передаются только термины **данного сегмента** | глоссарий студента — 100+ терминов: полный список удлиняет запрос в десятки раз и размывает внимание модели |
| Примеры few-shot — общие фразы про переменные | примеры берутся из **пула памяти переводов, не пересекающегося с тестом**; для каждого записывается тип решения | пересечение примеров и тестовых сегментов — утечка ответа; общие фразы не показывают решений предметной области |
| Системный промпт «по информатике» с правилом «не переводи Python-код» | промпт параметризован доменом варианта; правило обобщено: код, идентификаторы, пути, числа и единицы | варианты относятся к разным предметным областям |
| Проверка терминов подстрокой `tgt.lower() in target.lower()` | сравнение по основам с учётом словоизменения и беглой гласной + ручное подтверждение | подстрока не находит «наземную станцию» по термину «наземная станция»; в ЛР № 2 это давало ложные нарушения |
| Pydantic-модель с необязательными `used_terms`, `warnings` | все четыре поля обязательны, как в JSON Schema; добавлены смысловые проверки: `segment_id`, непустой перевод, язык | схема гарантирует синтаксис, но не смысл: пустой перевод или ответ на другом языке проходят её без ошибок |
| Repair без контроля объёма правки | после repair заново считаются нарушения и доля неизменённого текста (`unchanged_ratio`) | иначе нельзя проверить требование «не переписывай остальное» |
| Проверка Markdown и кода «на глаз» | проверочные случаи строятся из ваших сегментов: выделение, список, код с числом и путём; флаги сохранности | исходные сегменты обычно не содержат разметки, и проверять было бы нечего |
| Пример Yandex: `sdk.chat.completions(...)` «концептуально» | рабочий вызов `sdk.models.completions(...).configure(response_format={"json_schema": ...})` из актуальной документации | библиотека переименована в `yandex-ai-studio-sdk` (класс `AIStudio`); прежнее имя `yandex_cloud_ml_sdk` поддерживается как запасное |
| Пример GigaChat без модели и scope | указаны `model`, `scope`, настройка проверки сертификата | без `scope` авторизация физического лица не проходит; GigaChat использует сертификаты НУЦ Минцифры |
| Эталон в режиме `reference` с заранее записанными ответами | эталон работает на **локальной `qwen2.5:7b` через Ollama в Colab**; режим `mock` оставлен только для проверки кода | реальная модель без ключей воспроизводится у любого преподавателя; заранее записанные ответы нельзя выдавать за вывод модели |
| Заголовок «инструкции для Smartcat/API» | Smartcat из шагов убран | в работе нет шагов для Smartcat |

---

## 3. Входные данные

| Источник | Файл | Что берётся |
|---|---|---|
| ЛР № 1 | `variant_<N>.tmx` | тестовые сегменты (первые 12), эталонный перевод, пул примеров few-shot (остальные) |
| ЛР № 1 | `variant_<N>.tbx` | термины для промпта и автоматической QA |
| ЛР № 2 | `lab02_results.zip` → `results_marian.jsonl`, `results_nllb.jsonl` | переводы NMT на тех же сегментах для сравнения |

Файлы кладутся в каталог `DATA` через `files.upload()` или Google Drive (заготовки в блоке 2).
Результаты ЛР № 2 необязательны: без них работа выполняется полностью, пропускается только
сравнение с NMT.

**Минимальный объём:** 12 тестовых сегментов и не менее 2 сегментов в пуле few-shot, то есть
**14+ единиц перевода** в TMX. Корпус ЛР № 2 (120+ сегментов) подходит без изменений: первые 12
идут в тест, остальные образуют пул примеров.

---

## 4. Выбор модели и подключение

Студент выбирает **одну основную модель**; вторая может использоваться дополнительно.

### Локальная модель через Ollama (рекомендуется для Colab)

Ключи не нужны, текст не покидает среду. В блоке 1.1 ноутбука выполняется:

```bash
apt-get install -y zstd pciutils          # нужны установщику Ollama
curl -fsSL https://ollama.com/install.sh | sh
ollama serve &                            # сервер в фоне
ollama pull qwen2.5:7b                    # ≈ 4.7 ГБ, работает на T4
```

```python
from ollama import chat
response = chat(model="qwen2.5:7b", messages=messages, format=SCHEMA,
                options={"temperature": 0, "seed": 42})
raw_answer = response.message.content
```

Версия модели фиксируется по `digest` из `ollama list`.

### GigaChat

```python
from gigachat import GigaChat
from gigachat.models import Chat, Messages, MessagesRole

chat_request = Chat(
    model="GigaChat-2-Max",
    messages=[Messages(role=MessagesRole.SYSTEM, content=system_prompt),
              Messages(role=MessagesRole.USER, content=user_payload)],
    temperature=0.01,
    response_format={"type": "json_schema", "schema": SCHEMA, "strict": True},
)
with GigaChat(credentials=..., scope="GIGACHAT_API_PERS", verify_ssl_certs=False) as client:
    response = client.chat(chat_request)
raw_answer = response.choices[0].message.content
```

* ключ авторизации — в личном кабинете GigaChat API; для физических лиц scope `GIGACHAT_API_PERS`;
* `verify_ssl_certs=False` допустимо в учебной среде; в рабочей установите корневой сертификат НУЦ Минцифры;
* версия модели возвращается в поле `response.model`.

### Yandex AI Studio

```python
from yandex_ai_studio_sdk import AIStudio        # прежнее имя: yandex_cloud_ml_sdk.YCloudML

sdk = AIStudio(folder_id=..., auth=...)
model = (sdk.models.completions("yandexgpt", model_version="latest")
         .configure(temperature=0, max_tokens=800, response_format={"json_schema": SCHEMA}))
result = model.run([{"role": "system", "text": system_prompt},
                    {"role": "user", "text": user_payload}])
raw_answer = result[0].text
```

Нужны идентификатор каталога и API-ключ сервисного аккаунта с ролью `ai.languageModels.user`.
Альтернатива — OpenAI-совместимый эндпоинт `https://llm.api.cloud.yandex.net/v1` с моделью
`gpt://<folder_id>/yandexgpt/latest`.

### Секреты

| Переменная | Для чего |
|---|---|
| `GIGACHAT_CREDENTIALS`, `GIGACHAT_SCOPE` | GigaChat |
| `YANDEX_FOLDER_ID`, `YANDEX_API_KEY` | Yandex AI Studio |

В Colab секреты добавляются в **Colab Secrets** (значок ключа слева), локально — в `.env`. В
репозиторий попадает только `.env.example` без значений; `.env` исключён через `.gitignore`.

> Текст, отправленный в облачный API, покидает вашу среду. Персональные данные и закрытые
> материалы в облачные модели не отправляются.

**Официальная документация:**
GigaChat — <https://developers.sber.ru/docs/ru/gigachat/guides/structured-output> ·
Yandex AI Studio — <https://yandex.cloud/ru/docs/ai-studio/concepts/generation/structured-output> ·
Ollama — <https://ollama.com/blog/structured-outputs>

---

## 5. Ход работы

| Блок | Что делается |
|---:|---|
| 0–1 | установка зависимостей, параметры эксперимента (`PROVIDER`, модель, температура, зерно, версия промптов) |
| 1.1 | запуск Ollama и загрузка модели (при локальном варианте) |
| 2, 2.0 | загрузка артефактов ЛР № 1 и результатов ЛР № 2, разбор TMX/TBX |
| 3 | 12 тестовых сегментов и пул few-shot без пересечения; тип решения для каждого примера |
| 4 | системный промпт, zero-shot инструкция, шаблон repair, стили A и B; термины сегмента; версии и хэши |
| 5 | JSON Schema, модель Pydantic, трёхуровневая проверка ответа |
| 6 | единый вызов модели с повтором при невалидном ответе и трассировкой |
| 7 | три режима — zero-shot, controlled, few-shot — на одних и тех же сегментах |
| 8 | терминологическая QA для всех режимов и NMT из ЛР № 2; ручное подтверждение |
| 9 | проверка разметки, кода, чисел и модальности |
| 10 | targeted repair минимум для 3 сегментов с контролем объёма правки |
| 11 | стиль A (академический) против стиля B (инструктивный) минимум на 3 сегментах |
| 12 | сводная таблица и экспертные решения по 5+ сегментам |
| 13 | отчёт, версии промптов, зависимости, выгрузка |

### Три режима

| Режим | Системная инструкция | Термины сегмента | Примеры |
|---|---|:---:|:---:|
| **zero-shot** | «Переведи и верни JSON» | — | — |
| **controlled** | полная инструкция с доменом варианта | ✓ | — |
| **few-shot** | полная инструкция | ✓ | 2–4 пары «запрос → JSON-ответ» из пула |

Примеры few-shot подаются парами сообщений `user`/`assistant`, где ответ — корректный JSON: так
модель видит и перевод, и точный формат. Параметр `FEWSHOT_STRATEGY = "fuzzy"` подбирает
примеры по сходству с сегментом — так же, как нечёткий поиск в памяти переводов из ЛР № 1.

### JSON-ответ

```json
{
  "segment_id": "s001",
  "translation": "Полётный контроллер стабилизирует планёр во время автономного полёта.",
  "used_terms": [{"source": "flight controller", "target": "полётный контроллер"}],
  "warnings": []
}
```

Проверка трёхуровневая: синтаксис JSON → обязательные поля и типы (Pydantic) → смысловые правила
(совпадение `segment_id`, непустой перевод, доля кириллицы не ниже 50 %). Невалидный ответ
повторно запрашивается с указанием причины; число попыток сохраняется.

### Трассировка (`results/results_llm.jsonl`)

Для каждого вызова: `segment_id`, `provider`, `model`, `model_version`, `mode`, `style`,
`temperature`, `prompt_version`, `prompt_hash`, `source`, `translation`, `used_terms`,
`warnings`, `json_valid`, `attempts`, `errors`, `latency_s`. Секреты не сохраняются.

---

## 6. Варианты

**Отдельной таблицы вариантов у ЛР № 3 нет.** Номер варианта, предметная область и терминология
наследуются из ЛР № 1; перечень тем — в материалах ЛР № 1.

| Что задаёт вариант | Где проявляется |
|---|---|
| Предметная область | системный промпт (`SYSTEM_TEMPLATE` с доменом) |
| Терминология TBX | термины сегмента в промпте, автоматическая QA, repair |
| Память переводов TMX | тестовые сегменты, эталон, примеры few-shot |
| Нарушения глоссария из ЛР № 2 | термины, которые проверяются в первую очередь |

**Особое внимание для любого варианта:** многословные термины (их модели чаще передают
описательно), многозначные слова предметной области и термины, на которых в ЛР № 2 ошиблись
MarianMT и NLLB (`glossary_qa.csv`).

**Эталон преподавателя** (`lab_03_teacher.ipynb`) выполнен по **варианту 25 — «Авиастроение и
беспилотные авиационные системы»**: 14 единиц перевода и 18 терминов из ЛР № 1 встроены в
блок 2. Тест — `s001`–`s012`, пул few-shot — `s013` (составной термин и ложный друг
переводчика) и `s014` (многозначность *pitch* и нормативное «частота вращения»).

---

## 7. Обязательный минимум

1. реальная LLM (облачный API или локальная модель); режим `mock` не засчитывается;
2. не менее **12 одинаковых сегментов** во всех трёх режимах;
3. примеры few-shot **не пересекаются** с тестовыми сегментами;
4. JSON Schema + проверка Pydantic + смысловые проверки;
5. терминологическая QA с ручным подтверждением каждого срабатывания;
6. проверка разметки, кода, чисел и модальности;
7. targeted repair минимум для **3 сегментов** с контролем объёма правки;
8. минимум **3 стилистических сравнения**;
9. минимум **5 содержательных различий** с типом ошибки и аргументацией;
10. фиксация `model`, `model_version`, `temperature`, `seed`, `prompt_version`, хэшей промптов;
11. отсутствие секретов в Git (включая историю коммитов);
12. сопоставление с эталоном ЛР № 1 и, при наличии, с MarianMT и NLLB из ЛР № 2.

---

## 8. Результаты и структура репозитория

```text
lab03/
├── LAB_03.ipynb
├── prompts/
│   ├── prompt_system.txt
│   ├── prompt_repair.txt
│   ├── system_zero.txt
│   ├── system_style_A.txt
│   ├── system_style_B.txt
│   ├── fewshot.json
│   └── translation_schema.json
├── results/
│   ├── results_llm.jsonl
│   ├── terminology_qa.csv
│   ├── markup_checks.csv
│   ├── repair_cases.csv
│   ├── style_comparison.csv
│   └── comparison.csv
├── figures/
│   └── terminology_compliance.png
├── prompt_versions.md
├── report.md
├── requirements.txt
├── .env.example
└── .gitignore
```

Всё это собирает блок 13 в архив `lab03_results.zip`. Колонки `confirmed` в
`terminology_qa.csv` и `preferred`/`comment` в `comparison.csv` заполняются **вручную**.

### Содержание `report.md`

1. выбранная модель или API; 2. точная версия модели; 3. схема авторизации без ключей;
4. системный промпт и версия; 5. JSON Schema; 6. таблица zero/controlled/few-shot и сравнение с
NMT; 7. анализ 5+ ошибок; 8. terminology repair; 9. сравнение стилей; 10. ограничения и вывод.

---

## 9. Критериальный лист (20 баллов)

| Критерий | Состав | Баллы |
|---|---|---:|
| **Работоспособность пайплайна** | реальная LLM — 2 · три режима — 2 · JSON-валидация и repair — 2 | **6** |
| **Лингвистическая обработка и валидация** | соблюдение глоссария — 2 · разметка, код, числа, модальность — 2 · валидный JSON — 1 | **5** |
| **Анализ ошибок и аргументация** | 5+ различий — 2 · сравнение режимов и стилей — 2 · вывод — 1 | **5** |
| **Оформление и воспроизводимость** | версии промптов и модели — 1 · README — 1 · зависимости — 1 · отсутствие секретов — 1 | **4** |
| **Итого** |  | **20** |

**Снижения:**

* режим `mock` или заранее записанные ответы вместо реальной модели — работа не засчитывается;
* ключ или токен в репозитории или его истории — −4 балла и обязательный отзыв ключа;
* примеры few-shot пересекаются с тестовыми сегментами — −2 балла;
* в промпт передаётся весь глоссарий вместо терминов сегмента — −1 балл;
* ручные колонки не заполнены — −2 балла;
* не указаны версия модели и хэши промптов — −1 балл.

---

## 10. Типичные ошибки

| Ошибка | Следствие | Как избежать |
|---|---|---|
| Примеры few-shot взяты из тестовых сегментов | few-shot «угадывает» ответ, сравнение режимов недостоверно | брать примеры только из пула; в коде стоит проверка |
| Весь глоссарий в каждом запросе | длинный запрос, рост стоимости, размытое внимание | `relevant_terms()` — только термины сегмента |
| Проверка терминов подстрокой | ложные нарушения из-за падежей | сравнение по основам + ручное подтверждение |
| Замена терминов `str.replace()` | несогласованные падежи («проверяет наземная станция») | repair через модель, проверка кодом |
| Доверие JSON без проверки | пустой перевод или ответ на другом языке | трёхуровневая проверка |
| Температура выше 0 | результаты режимов несравнимы между запусками | `temperature=0` (GigaChat — 0.01) и фиксированное зерно |
| Не зафиксирована версия модели | повторить эксперимент невозможно | `digest` Ollama или поле версии в ответе API |
| Ключ в коде ноутбука | компрометация ключа | Colab Secrets или `.env` вне репозитория |
| Персональные данные в облачном API | нарушение 152-ФЗ | такие тексты — только в локальную модель |

---

## 11. Состав материалов

| Файл | Назначение |
|---|---|
| `readme_lab_03.md` | настоящее описание работы |
| `lab_03_student.ipynb` | заготовка: теория, адаптер ЛР № 1–2, шаблоны функций с `TODO`, автотесты, места для экспертных решений и выводов |
| `lab_03_teacher.ipynb` | эталон варианта 25 на локальной `qwen2.5:7b`: полный пайплайн, пояснения к каждому блоку, методические указания |

Оба ноутбука содержат разделы «Суть работы за пять минут», «Связь с ЛР № 1–2», «Теоретическое
обоснование» и список литературы: от работ о промптинге LLM для перевода (Vilar et al., 2023) и
адаптивном переводе с памятью переводов (Moslem et al., 2023) до словарного промптинга
(Ghazvininejad et al., 2023) и управляемой генерации по схеме (Willard & Louf, 2023).
